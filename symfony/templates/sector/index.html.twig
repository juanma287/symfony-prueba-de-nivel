{% extends 'base.html.twig' %}

{% block title %}Listado de Sectores{% endblock %}

{% block body %}
    <div class="container-fluid">

        <!-- Page-Title -->
        <div class="row">
            <div class="col-sm-12">
                <div class="page-title-box">
                    <div class="float-right">
                        <ol class="breadcrumb">
                            <li class="breadcrumb-item"><a href="#">Sectores</a></li>
                            <li class="breadcrumb-item"><a href="#">Listado de Sectores</a></li>
                        </ol><!--end breadcrumb-->
                    </div><!--end /div-->
                    <h4 class="page-title">Listado de Sectores</h4>
                </div><!--end page-title-box-->
            </div><!--end col-->
        </div><!--end row-->
        <!-- end page title end breadcrumb -->


        <div class="row">
            <div class="col-lg-12">
               <div class="card">
                 <div class="row">
                  <div class="table-responsive col-md-12">
                   <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="thead-light">
                            <tr>
                                <th>Id</th>
                                <th>Nombre</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for sector in sectors %}
                            <tr>
                              
                                <td>{{ sector.id }}</td>
                                <td>{{ sector.nombre }}</td>
                                <td class="actions">
                                  <div class="row">
                                    <a 
                                    title='Mostrar'
                                    href="{{ path('sector_show', {'id': sector.id}) }}" 
                                    class="mr-2">
                                    <i class="fas fa-eye text-info font-16" style="padding-top: 3px;"></i>
                                    </a>
                                    <a 
                                    title='Editar'
                                    href="{{ path('sector_edit', {'id': sector.id}) }}" 
                                    class="mr-2">
                                    <i class="fas fa-edit text-info font-16" style="padding-top: 3px;"></i>
                                    </a>
                                    {{ include('sector/_delete_form.html.twig') }}
                                  </div>
                                </td>
                                
                            </tr>
                        {% else %}
                            <tr>
                                <td colspan="3">no hay sectores creados</td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                    </div>
                  </div>
                </div>


               <hr class="my-4">

                        <div id="page-content">
                            <div class="content-box">

                                <div class="content-box-wrapper">


                                    <div class="form-group" style="margin: 10px;">
                                        <a class="btn btn-outline-success waves-effect waves-light" href="{{ path('sector_new') }}">
                                            <i class="glyph-icon icon-list" aria-hidden="true"></i>
                                            Crear sector
                                        </a>
                                      
                                    </div>

                                </div>
                            </div>
                        </div>
   

                    <div class="row">
                        <div class="col-md-12" style="display: inline-block;">
                            {% for type, flashMessages in app.session.flashbag.all() %}
                                    {% for flashMessage in flashMessages %}
                                        <div class="alert alert-{{ type }}" style="margin: 10px 0px 10px 0px">
                                                {{ flashMessage|raw }}
                                        </div>
                                    {% endfor %}
                                {% endfor %}
                            </div>
                        </div>
                </div>
            </div>
          </div>
        </div>
 </div>
{% endblock %}
